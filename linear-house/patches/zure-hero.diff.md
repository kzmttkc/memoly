# /zure ヒーロー差し替えメモ

## 目的

折りたたみ上を約束＋成果物だけにする。トークン（40×12・ink）を固定する。

## 変更

1. 最上段の期限バーを消す（日数は見本メタへ）
2. ドロップと見本を同じ `lh-frame`（12px）へ
3. LegalFold は閉じているとき何も出さない（「選んだあとに表示」行を消す）
4. 「登録不要」の二重と長いリードを削る
5. サンプルは枠内・ドロップの後
6. 「本文を貼る」は枠の外の下線
7. `house-lock.css` でボタン角丸999を抑止

## 触らない

- 下の「置く→見る→残す」と比較表（ZureLpBelow から見本だけ外す）
- Agent面の家（クリーム地は触らない）。主CTA案は `agent-cta.html` のみ
