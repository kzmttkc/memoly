# LP表示の根拠記録（2026-07-23〜24 ドッグフーディング）

`docs/BANTO_LP_CLAIM_EVIDENCE.md` が参照する一次記録の**保全コピー**。

## なぜここに置くか

景表法7条2項（不実証広告規制）で合理的根拠の提出を求められたとき、**期限は15日**。
それまでこの記録は `~/Takeshi_Automation/output/0723/` にしか無く、そこは
`.gitignore` で除外された日付ディレクトリだった（実測: `git check-ignore` が
`output/` にヒット）。つまり **git 履歴にもリモートにも1バイトも入っておらず**、
この Mac の1台と、日付ディレクトリの整理作業だけが根拠の生死を握っていた。

「25分/10分」「約30秒」等の速さの表示は現在も公開中なので、根拠を先に不滅にする。

## 何をコピーしたか（2026-07-30）

| 置き場所 | 中身 |
|---|---|
| `loop01/` | 2026-07-23 のペルソナ10名の実操作記録（`persona_01..10.md` ほか） |
| `loop02/` | 2026-07-24 の同（再実測） |
| `banto_10ペルソナUX徹底監査_L1..L3.md` | 統合報告。表示9番はここにしか秒数が無い |

Markdown のみ・計372KB。個人情報は含まない（登録に使ったアドレスは全て
`test+…@example.com` であることを確認済み）。

## コピーしていないもの

スクリーンショット（`persona_09_shots/`・`persona_05_evidence/`・`evidence/` ほか・
合計約63MB）は元の場所にのみ存在する。`BANTO_LP_CLAIM_EVIDENCE.md` の
「会社ごとにデータが分離される（403）」「退会後はログインできない」の2件は画像が
一次証拠なので、**元ディレクトリを消すときはこの2件の画像だけは別途退避すること**。

元の場所: `~/Takeshi_Automation/output/0723/dogfood_loop01|02/`
